[build]
  command = "npm run build"
  publish = "dist"

# API routes → Netlify Functions
[[redirects]]
  from = "/api/posts"
  to = "/.netlify/functions/posts"
  status = 200

# SPA fallback
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
